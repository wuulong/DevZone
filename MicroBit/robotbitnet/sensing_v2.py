# Add your Python code here. E.g.
from microbit import *

cnt=8
while True:
    # unit test
    #print(display.read_light_level())
    #cnt+=1
    #display.show(cnt)
    #print(accelerometer.get_values())
    #print(compass.heading())
    #print(microphone.sound_level())
    #print(int(pin_logo.is_touched()))
    #pack
    #a_x,a_y,a_z = accelerometer.get_values()
    #print("%i,%i,%i,%i,%i,%i" %(int(pin_logo.is_touched()),display.read_light_level(),compass.heading(),a_x,a_y,a_z))
    sleep(100)
